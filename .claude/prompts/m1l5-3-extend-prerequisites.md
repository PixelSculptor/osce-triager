```yaml
name: Extend plan with prerequisites - how to configure CLI for {{platform_name + extra tooling}}
description: This plan extends the current plan by adding prerequisites for configuring the CLI for {{platform_name + extra tooling}}.
license: CC BY-NC-ND 4.0
metadata:
  tags:
    - cli
    - configuration
    - prerequisites
    - {{platform_name}}
    - {{extra_tooling}}
```

Proszę rozszerzyć plan o wymagania wstępne - jak skonfigurować CLI dla {{platform_name + extra tooling}}